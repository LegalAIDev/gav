# dependencies
node_modules/

# build output
dist/

# Image/audio assets now live in the repo under public/assets/{images,audio}
# (committed). `npm run fetch-assets` remains available to (re)populate them
# from the Monster Tamer release if they're ever missing.
.asset-cache/

# editor / OS
.DS_Store
*.log
